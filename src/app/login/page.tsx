import Link from "next/link";
import { LoginForm } from "@/app/login/login-form";

export default function LoginPage() { return <main className="grid min-h-screen place-items-center bg-canvas p-6"><section className="w-full max-w-md rounded-panel border border-border bg-surface p-8 shadow-elevated"><p className="text-sm font-semibold tracking-[0.14em] text-brand-primary">PLAYBOOK<span className="ml-2 text-[10px] text-brand-accent">FOS</span></p><h1 className="mt-6 text-2xl font-semibold tracking-[-0.035em] text-text-primary">Financial Operating System</h1><p className="mt-2 text-sm leading-6 text-text-muted">Only approved PLAYBOOK users can access the dashboard. Sign in with your individual Google account.</p><LoginForm /><p className="mt-4 text-xs leading-5 text-text-muted">Your role and allowlist status are verified server-side after Google returns.</p><Link href="/access-denied" className="mt-6 block text-sm font-semibold text-brand-accent">View access-denied state →</Link></section></main>; }
