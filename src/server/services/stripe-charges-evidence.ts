import type { StripeEvidenceRow } from "@/server/repositories/stripe-charges-evidence-repository";
export type AdminStripeEvidenceRecord = { evidenceId: string; sourceRowNumber: number; sourceEntryKey: "primary" | "refund"; transactionKind: "sale" | "refund" | "needs_review"; chargeId: string | null; occurredAt: string | null; occurredAtRaw: string | null; originalCurrency: string; originalAmount: string | null; customerName: string | null; customerEmail: string | null; customerPhone: string | null; sourceStatus: string | null; };
export function toAdminStripeEvidenceRecord(row: StripeEvidenceRow): AdminStripeEvidenceRecord { return { evidenceId: row.id, sourceRowNumber: row.source_row_number, sourceEntryKey: row.source_entry_key, transactionKind: row.transaction_kind, chargeId: row.provider_payment_id, occurredAt: row.occurred_at, occurredAtRaw: row.occurred_at_raw, originalCurrency: row.original_currency, originalAmount: row.credit_amount ?? row.debit_amount, customerName: row.customer_name, customerEmail: row.customer_email, customerPhone: row.customer_phone, sourceStatus: row.description_raw }; }
