import { SalesTrendChart } from "@/components/charts";
import { AppShell } from "@/components/app-shell";
import { DateRangeSelector, MetricCard, NotBackfilledState, ProgressMetric, SectionCard, StatusBadge } from "@/components/ui";
import { executiveMetrics, risksAndOpportunities, salesTrend } from "@/mocks/executive";

export function ExecutiveDashboard() { return <AppShell title="Executive overview" description="A 60-second view of operating performance. Sales and B2B bookings remain intentionally separate." controls={<DateRangeSelector />}><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{executiveMetrics.map((metric, index) => <MetricCard key={metric.label} {...metric} emphasis={index === 0} />)}</div><div className="mt-4 grid gap-4 xl:grid-cols-[1.65fr_1fr]"><SectionCard title="Six-month sales trend" description="Recognised sales by revenue stream; bookings are excluded."><SalesTrendChart data={salesTrend} /></SectionCard><SectionCard title="Year-to-date progress" description="Recognised sales against annual targets"><div className="space-y-5"><ProgressMetric label="Total recognised sales" value="$1.02m" target="$1.80m" progress={57} /><ProgressMetric label="B2C recognised sales" value="$561k" target="$900k" progress={62} /><ProgressMetric label="B2B recognised sales" value="$459k" target="$900k" progress={51} /></div></SectionCard><SectionCard title="Risks & opportunities" description="Prioritised items for management attention"><ul className="space-y-4">{risksAndOpportunities.map((item) => <li key={item.title} className="flex gap-3"><StatusBadge status={item.type} /><div><p className="text-sm font-medium text-text-primary">{item.title}</p><p className="mt-0.5 text-sm leading-6 text-text-muted">{item.detail}</p></div></li>)}</ul></SectionCard></div><div className="mt-4"><NotBackfilledState title="Historical expense trends not loaded" description="Expense comparisons before January 2026 are not yet backfilled and are excluded from this overview." /></div></AppShell>; }
