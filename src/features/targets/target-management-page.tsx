"use client";

import { useEffect, useState } from "react";
import { AppShell } from "@/components/app-shell";
import { EmptyState, ErrorState, LoadingSkeleton, SectionCard } from "@/components/ui";
import { useCanManage } from "@/lib/auth/role-context";

type TargetResponse = {
  financialTargets: Array<{ id: string; metric_code: string; period_start: string; period_end: string; target_amount_usd: string; finance_reference: string }>;
  operationalTargets: Array<{ id: string; display_name: string; value_kind: "money_usd" | "quantity"; target_value: string; unit_label: string | null; period_start: string; period_end: string; finance_reference: string }>;
  operationalProgressUpdates: Array<{ id: string; target_id: string; actual_value: string; effective_on: string; evidence_note: string }>;
};
type FinancialTarget = TargetResponse["financialTargets"][number];
type OperationalTarget = TargetResponse["operationalTargets"][number];

function label(value: string) { return value.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase()); }
function usd(value: string) { return "$" + Number(value).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }

export function TargetManagementPage() {
  const [targets, setTargets] = useState<TargetResponse | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [showCustomForm, setShowCustomForm] = useState(false);
  const [showFinancialForm, setShowFinancialForm] = useState(false);
  const [progressTarget, setProgressTarget] = useState<TargetResponse["operationalTargets"][number] | null>(null);
  const [revisionTarget, setRevisionTarget] = useState<FinancialTarget | null>(null);
  const [operationalRevisionTarget, setOperationalRevisionTarget] = useState<OperationalTarget | null>(null);
  const [operationalRevisionValueKind, setOperationalRevisionValueKind] = useState<"money_usd" | "quantity">("quantity");
  const [saving, setSaving] = useState(false);
  const [saveError, setSaveError] = useState<string | null>(null);
  const canManage = useCanManage();

  useEffect(() => {
    void fetch("/api/targets").then(async (response) => {
      const body = await response.json();
      if (!response.ok) throw new Error(body.error ?? "Could not load targets.");
      setTargets(body);
    }).catch((cause: unknown) => setError(cause instanceof Error ? cause.message : "Could not load targets."));
  }, []);

  async function createCustomTarget(form: HTMLFormElement) {
    setSaving(true); setSaveError(null);
    const values = new FormData(form);
    const valueKind = String(values.get("valueKind"));
    const response = await fetch("/api/admin/targets/operational", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({
      displayName: values.get("displayName"), valueKind, targetValue: values.get("targetValue"), unitLabel: valueKind === "quantity" ? values.get("unitLabel") : undefined,
      periodStart: values.get("periodStart"), periodEnd: values.get("periodEnd"), status: "active", financeReference: values.get("financeReference"), revisionReason: values.get("revisionReason"),
    }) });
    const body = await response.json();
    if (!response.ok) { setSaveError(body.error ?? "The target could not be saved."); setSaving(false); return; }
    const refreshed = await fetch("/api/targets").then((result) => result.json());
    setTargets(refreshed); setShowCustomForm(false); setSaving(false); form.reset();
  }

  async function createFinancialTarget(form: HTMLFormElement) {
    setSaving(true); setSaveError(null);
    const values = new FormData(form);
    const response = await fetch("/api/admin/targets/financial", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({
      metricCode: values.get("metricCode"), targetAmountUsd: values.get("targetAmountUsd"), periodStart: values.get("periodStart"), periodEnd: values.get("periodEnd"),
      status: "active", financeReference: values.get("financeReference"), revisionReason: values.get("revisionReason"),
    }) });
    const body = await response.json();
    if (!response.ok) { setSaveError(body.error ?? "The financial target could not be saved."); setSaving(false); return; }
    const refreshed = await fetch("/api/targets").then((result) => result.json());
    setTargets(refreshed); setShowFinancialForm(false); setSaving(false); form.reset();
  }

  async function recordProgress(form: HTMLFormElement) {
    if (!progressTarget) return;
    setSaving(true); setSaveError(null);
    const values = new FormData(form);
    const response = await fetch("/api/admin/targets/operational/" + progressTarget.id + "/progress", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ actualValue: values.get("actualValue"), effectiveOn: values.get("effectiveOn"), evidenceNote: values.get("evidenceNote") }) });
    const body = await response.json();
    if (!response.ok) { setSaveError(body.error ?? "The progress update could not be saved."); setSaving(false); return; }
    const refreshed = await fetch("/api/targets").then((result) => result.json());
    setTargets(refreshed); setProgressTarget(null); setSaving(false); form.reset();
  }

  async function reviseFinancialTarget(form: HTMLFormElement) {
    if (!revisionTarget) return;
    setSaving(true); setSaveError(null);
    const values = new FormData(form);
    const response = await fetch("/api/admin/targets/financial/" + revisionTarget.id + "/revise", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ metricCode: revisionTarget.metric_code, targetAmountUsd: values.get("targetAmountUsd"), periodStart: values.get("periodStart"), periodEnd: values.get("periodEnd"), status: "active", financeReference: values.get("financeReference"), revisionReason: values.get("revisionReason") }) });
    const body = await response.json();
    if (!response.ok) { setSaveError(body.error ?? "The target could not be revised."); setSaving(false); return; }
    const refreshed = await fetch("/api/targets").then((result) => result.json());
    setTargets(refreshed); setRevisionTarget(null); setSaving(false); form.reset();
  }

  async function reviseOperationalTarget(form: HTMLFormElement) {
    if (!operationalRevisionTarget) return;
    setSaving(true); setSaveError(null);
    const values = new FormData(form);
    const valueKind = String(values.get("valueKind"));
    const response = await fetch("/api/admin/targets/operational/" + operationalRevisionTarget.id + "/revise", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({
      displayName: values.get("displayName"), valueKind, targetValue: values.get("targetValue"), unitLabel: valueKind === "quantity" ? values.get("unitLabel") : undefined,
      periodStart: values.get("periodStart"), periodEnd: values.get("periodEnd"), status: "active", financeReference: values.get("financeReference"), revisionReason: values.get("revisionReason"),
    }) });
    const body = await response.json();
    if (!response.ok) { setSaveError(body.error ?? "The operational target could not be revised."); setSaving(false); return; }
    const refreshed = await fetch("/api/targets").then((result) => result.json());
    setTargets(refreshed); setOperationalRevisionTarget(null); setSaving(false); form.reset();
  }

  return <AppShell title="Targets" description="Approved financial goals and auditable operational goals. Financial actuals remain unavailable until verified source history is complete.">
    {error ? <ErrorState title="Targets unavailable" description={error} /> : !targets ? <LoadingSkeleton rows={4} /> : <>
      <SectionCard title="Financial targets" description="Goals approved by Finance. Actuals are calculated only from verified source records.">
        {canManage && <button type="button" onClick={() => setShowFinancialForm(true)} className="mb-4 min-h-11 rounded-md bg-brand-primary px-4 py-2 text-sm font-semibold text-white hover:bg-brand-primary/90">Add financial target</button>}
        <p className="rounded-md bg-surface-muted px-3 py-2 text-sm font-medium text-text-secondary">Actuals not fully loaded</p>
        <p className="mt-2 text-sm text-text-muted">Financial progress will appear only after B2B and B2C history is complete and reconciled. Missing data is never treated as zero.</p>
        <div className="mt-5 space-y-3">{targets.financialTargets.length ? targets.financialTargets.map((target) => <div key={target.id} className="rounded-card border border-border p-4"><div className="flex justify-between gap-3"><div><p className="font-semibold text-text-primary">{label(target.metric_code)}</p><p className="mt-1 text-sm text-text-muted">{target.period_start} to {target.period_end} · {target.finance_reference}</p></div>{canManage && <button type="button" onClick={() => setRevisionTarget(target)} className="min-h-11 rounded-md border border-brand-primary px-3 text-sm font-semibold text-brand-primary">Revise target</button>}</div><p className="mt-3 text-xl font-semibold tabular-nums text-text-primary">{usd(target.target_amount_usd)}</p></div>) : <EmptyState title="No financial targets yet" description="An Admin can add an approved target when Finance confirms the metric and value." />}</div>
      </SectionCard>
      <SectionCard title="Operational targets" description="Manual operational metrics are kept separate from financial reporting." className="mt-4">
        {canManage && <button type="button" onClick={() => setShowCustomForm(true)} className="mb-4 min-h-11 rounded-md bg-brand-primary px-4 py-2 text-sm font-semibold text-white hover:bg-brand-primary/90">Add custom target</button>}
        <p className="text-xs font-semibold uppercase tracking-wide text-text-muted">Manual operational metric</p>
        <div className="mt-4 space-y-3">{targets.operationalTargets.length ? targets.operationalTargets.map((target) => { const latest = targets.operationalProgressUpdates.find((update) => update.target_id === target.id); return <div key={target.id} className="rounded-card border border-border p-4"><div className="flex justify-between gap-3"><div><p className="font-semibold text-text-primary">{target.display_name}</p><p className="mt-1 text-sm text-text-muted">{target.period_start} to {target.period_end} · {target.finance_reference}</p></div>{canManage && <div className="flex shrink-0 flex-col gap-2 sm:flex-row"><button type="button" onClick={() => { setOperationalRevisionTarget(target); setOperationalRevisionValueKind(target.value_kind); }} className="min-h-11 rounded-md border border-brand-primary px-3 text-sm font-semibold text-brand-primary">Revise target</button><button type="button" onClick={() => setProgressTarget(target)} className="min-h-11 rounded-md border border-brand-primary px-3 text-sm font-semibold text-brand-primary">Update progress</button></div>}</div><p className="mt-3 text-xl font-semibold tabular-nums text-text-primary">{target.value_kind === "money_usd" ? usd(target.target_value) : target.target_value + " " + target.unit_label}</p>{latest ? <p className="mt-2 text-sm text-text-secondary">Latest manual progress: {latest.actual_value} · {latest.effective_on} · {latest.evidence_note}</p> : <p className="mt-2 text-sm text-text-muted">No manual progress recorded yet.</p>}</div>; }) : <EmptyState title="No operational targets yet" description="Custom targets, such as tickets or sponsorships, will appear here after an Admin creates one." />}</div>
      </SectionCard>
      {showCustomForm && <div className="fixed inset-0 z-50 grid place-items-center bg-brand-primary/40 p-4"><form aria-label="Add custom target" onSubmit={(event) => { event.preventDefault(); void createCustomTarget(event.currentTarget); }} className="w-full max-w-xl rounded-card bg-surface p-6 shadow-elevated"><h2 className="text-xl font-semibold text-text-primary">Add custom target</h2><p className="mt-2 text-sm text-text-muted">This stays separate from financial reporting.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm font-medium text-text-secondary sm:col-span-2">Name<input required name="displayName" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Type<select name="valueKind" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary"><option value="quantity">Quantity</option><option value="money_usd">USD money</option></select></label><label className="text-sm font-medium text-text-secondary">Goal<input required name="targetValue" inputMode="decimal" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Unit (for quantity)<input name="unitLabel" placeholder="tickets" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Start date<input required name="periodStart" type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">End date<input required name="periodEnd" type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary sm:col-span-2">Reference<input required name="financeReference" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary sm:col-span-2">Reason<input required name="revisionReason" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label></div>{saveError && <p role="alert" className="mt-3 text-sm text-danger">{saveError}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setShowCustomForm(false)} className="min-h-11 rounded-md border border-border px-4 text-sm font-semibold text-text-secondary">Cancel</button><button disabled={saving} type="submit" className="min-h-11 rounded-md bg-brand-primary px-4 text-sm font-semibold text-white disabled:opacity-60">{saving ? "Saving…" : "Create target"}</button></div></form></div>}
      {showFinancialForm && <div className="fixed inset-0 z-50 grid place-items-center bg-brand-primary/40 p-4"><form aria-label="Add financial target" onSubmit={(event) => { event.preventDefault(); void createFinancialTarget(event.currentTarget); }} className="w-full max-w-xl rounded-card bg-surface p-6 shadow-elevated"><h2 className="text-xl font-semibold text-text-primary">Add financial target</h2><p className="mt-2 text-sm text-text-muted">This creates a Finance-approved goal only. Actuals remain source-calculated.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm font-medium text-text-secondary sm:col-span-2">Metric<select required name="metricCode" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary"><option value="b2c_cash_received">B2C cash received</option><option value="b2b_bookings">B2B bookings</option><option value="b2b_recognised_sales">B2B recognised sales</option><option value="total_recognised_sales">Total recognised sales</option></select></label><label className="text-sm font-medium text-text-secondary">USD goal<input required name="targetAmountUsd" inputMode="decimal" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Start date<input required name="periodStart" type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">End date<input required name="periodEnd" type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary sm:col-span-2">Finance reference<input required name="financeReference" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary sm:col-span-2">Reason<input required name="revisionReason" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label></div>{saveError && <p role="alert" className="mt-3 text-sm text-danger">{saveError}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setShowFinancialForm(false)} className="min-h-11 rounded-md border border-border px-4 text-sm font-semibold text-text-secondary">Cancel</button><button disabled={saving} type="submit" className="min-h-11 rounded-md bg-brand-primary px-4 text-sm font-semibold text-white disabled:opacity-60">{saving ? "Saving…" : "Create financial target"}</button></div></form></div>}
      {progressTarget && <div className="fixed inset-0 z-50 grid place-items-center bg-brand-primary/40 p-4"><form aria-label="Update operational progress" onSubmit={(event) => { event.preventDefault(); void recordProgress(event.currentTarget); }} className="w-full max-w-xl rounded-card bg-surface p-6 shadow-elevated"><h2 className="text-xl font-semibold text-text-primary">Update progress: {progressTarget.display_name}</h2><p className="mt-2 text-sm text-text-muted">This is an audited manual operational update and does not affect financial totals.</p><div className="mt-5 grid gap-3"><label className="text-sm font-medium text-text-secondary">Actual value<input required name="actualValue" inputMode="decimal" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Effective date<input required name="effectiveOn" type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Evidence note<input required name="evidenceNote" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label></div>{saveError && <p role="alert" className="mt-3 text-sm text-danger">{saveError}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setProgressTarget(null)} className="min-h-11 rounded-md border border-border px-4 text-sm font-semibold text-text-secondary">Cancel</button><button disabled={saving} type="submit" className="min-h-11 rounded-md bg-brand-primary px-4 text-sm font-semibold text-white disabled:opacity-60">{saving ? "Saving…" : "Save progress"}</button></div></form></div>}
      {operationalRevisionTarget && <div className="fixed inset-0 z-50 grid place-items-center bg-brand-primary/40 p-4"><form aria-label="Revise operational target" onSubmit={(event) => { event.preventDefault(); void reviseOperationalTarget(event.currentTarget); }} className="w-full max-w-xl rounded-card bg-surface p-6 shadow-elevated"><h2 className="text-xl font-semibold text-text-primary">Revise {operationalRevisionTarget.display_name}</h2><p className="mt-2 text-sm text-text-muted">The current target will be archived and a replacement created in the same audited action. Manual progress stays in history.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm font-medium text-text-secondary sm:col-span-2">Name<input required name="displayName" defaultValue={operationalRevisionTarget.display_name} className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Type<select name="valueKind" value={operationalRevisionValueKind} onChange={(event) => setOperationalRevisionValueKind(event.target.value as "money_usd" | "quantity")} className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary"><option value="quantity">Quantity</option><option value="money_usd">USD money</option></select></label><label className="text-sm font-medium text-text-secondary">Goal<input required name="targetValue" defaultValue={operationalRevisionTarget.target_value} inputMode="decimal" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label>{operationalRevisionValueKind === "quantity" && <label className="text-sm font-medium text-text-secondary">Unit<input required name="unitLabel" defaultValue={operationalRevisionTarget.unit_label ?? ""} className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label>}<label className="text-sm font-medium text-text-secondary">Start date<input required name="periodStart" defaultValue={operationalRevisionTarget.period_start} type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">End date<input required name="periodEnd" defaultValue={operationalRevisionTarget.period_end} type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary sm:col-span-2">Reference<input required name="financeReference" defaultValue={operationalRevisionTarget.finance_reference} className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary sm:col-span-2">Revision reason<input required name="revisionReason" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label></div>{saveError && <p role="alert" className="mt-3 text-sm text-danger">{saveError}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setOperationalRevisionTarget(null)} className="min-h-11 rounded-md border border-border px-4 text-sm font-semibold text-text-secondary">Cancel</button><button disabled={saving} type="submit" className="min-h-11 rounded-md bg-brand-primary px-4 text-sm font-semibold text-white disabled:opacity-60">{saving ? "Saving…" : "Revise target"}</button></div></form></div>}
      {revisionTarget && <div className="fixed inset-0 z-50 grid place-items-center bg-brand-primary/40 p-4"><form aria-label="Revise financial target" onSubmit={(event) => { event.preventDefault(); void reviseFinancialTarget(event.currentTarget); }} className="w-full max-w-xl rounded-card bg-surface p-6 shadow-elevated"><h2 className="text-xl font-semibold text-text-primary">Revise {label(revisionTarget.metric_code)}</h2><p className="mt-2 text-sm text-text-muted">The current target will be archived and a replacement created in the same audited action.</p><div className="mt-5 grid gap-3"><label className="text-sm font-medium text-text-secondary">USD goal<input required name="targetAmountUsd" defaultValue={revisionTarget.target_amount_usd} inputMode="decimal" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Start date<input required name="periodStart" defaultValue={revisionTarget.period_start} type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">End date<input required name="periodEnd" defaultValue={revisionTarget.period_end} type="date" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Finance reference<input required name="financeReference" defaultValue={revisionTarget.finance_reference} className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label><label className="text-sm font-medium text-text-secondary">Revision reason<input required name="revisionReason" className="mt-1 h-11 w-full rounded-md border border-border bg-surface px-3 text-text-primary" /></label></div>{saveError && <p role="alert" className="mt-3 text-sm text-danger">{saveError}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setRevisionTarget(null)} className="min-h-11 rounded-md border border-border px-4 text-sm font-semibold text-text-secondary">Cancel</button><button disabled={saving} type="submit" className="min-h-11 rounded-md bg-brand-primary px-4 text-sm font-semibold text-white disabled:opacity-60">{saving ? "Saving…" : "Revise target"}</button></div></form></div>}
    </>}
  </AppShell>;
}
