"use client";
import { Cell, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { TrendPoint } from "@/types/dashboard";

const colors = ["#174b3b", "#6da884", "#d3a459", "#94a3b8", "#8b5e3c"];
export function SalesTrendChart({ data }: { data: TrendPoint[] }) { return <div className="h-64" aria-label="Six-month sales trend chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{ top: 5, right: 8, left: 0, bottom: 0 }}><XAxis dataKey="month" tickLine={false} axisLine={false} tick={{ fill: "#64748b", fontSize: 12 }} /><YAxis tickFormatter={(value: number) => `$${value / 1000}k`} tickLine={false} axisLine={false} tick={{ fill: "#64748b", fontSize: 12 }} /><Tooltip formatter={(value: number) => [`$${value.toLocaleString()}`, ""]} /><Legend /><Line type="monotone" dataKey="b2c" name="B2C recognised" stroke="#174b3b" strokeWidth={2} dot={false} /><Line type="monotone" dataKey="b2b" name="B2B recognised" stroke="#6da884" strokeWidth={2} dot={false} /><Line type="monotone" dataKey="other" name="Other" stroke="#d3a459" strokeWidth={2} dot={false} /></LineChart></ResponsiveContainer></div>; }
export function BreakdownChart({ data }: { data: Array<{ name: string; value: number }> }) { return <div className="h-52" aria-label="Breakdown chart"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={50} outerRadius={78} paddingAngle={2}>{data.map((entry, index) => <Cell key={entry.name} fill={colors[index % colors.length]} />)}</Pie><Tooltip formatter={(value: number) => value > 1000 ? `$${value.toLocaleString()}` : `${value}%`} /><Legend verticalAlign="bottom" iconType="circle" /></PieChart></ResponsiveContainer></div>; }
