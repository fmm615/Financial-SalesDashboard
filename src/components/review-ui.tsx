"use client";
import { X } from "lucide-react";
import type { ReviewFlag, ReviewItem } from "@/types/dashboard";
import { StatusBadge } from "@/components/ui";

export { FilterBar, SectionCard } from "@/components/ui";
export function ReviewFlagBadge({ type }: { type: ReviewFlag }) { return <StatusBadge status={type} />; }
export function DetailDrawer({ item, onClose }: { item: ReviewItem; onClose: () => void }) { const duplicate = item.type === "Possible duplicate"; return <div className="fixed inset-0 z-40 bg-ink/30" role="presentation"><aside role="dialog" aria-modal="true" aria-labelledby="review-detail-title" className="absolute inset-y-0 right-0 w-full max-w-xl overflow-y-auto bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-medium text-slate-500">{item.id}</p><h2 id="review-detail-title" className="mt-1 text-xl font-medium text-ink">{item.title}</h2><div className="mt-3"><ReviewFlagBadge type={item.type} /></div></div><button type="button" aria-label="Close review detail" onClick={onClose} className="p-2 text-slate-600"><X /></button></div><dl className="mt-7 space-y-4 text-sm"><div><dt className="text-slate-500">Original record</dt><dd className="mt-1 font-medium text-ink">{item.record}</dd></div><div><dt className="text-slate-500">Reason for flag</dt><dd className="mt-1 text-slate-700">{item.reason}</dd></div><div><dt className="text-slate-500">Suggested action</dt><dd className="mt-1 text-slate-700">{item.suggestedAction}</dd></div></dl><div className="mt-7"><h3 className="font-medium text-ink">Related records</h3><div className={`mt-3 grid gap-3 ${duplicate ? "sm:grid-cols-2" : ""}`}>{item.related.map((related) => <div key={related} className="border border-line p-4 text-sm text-slate-700">{related}</div>)}</div></div><div className="mt-7 border-t border-line pt-5"><h3 className="font-medium">Resolution history</h3><p className="mt-2 text-sm text-slate-500">No live resolution actions are enabled in this UI phase.</p><label className="mt-4 block text-sm font-medium text-slate-700">Resolution note<textarea className="mt-2 min-h-20 w-full border border-line p-3 font-normal" placeholder="Required when this flag is cleared" /></label><button type="button" className="mt-4 border border-forest px-4 py-2 text-sm font-medium text-forest">Mark as reviewed</button></div></aside></div>; }
